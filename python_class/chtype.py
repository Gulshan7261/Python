def check_character(ch):
    if ch.isdigit():
        print(f"{ch} is a Digit")
    elif ch.islower():
        print(f"{ch} is a lowercase Latter")
    elif ch.isupper():
        print(f"{ch} is a lowercase Latter")

    else:
        print(f"{ch} is a lowercase Latter")

char = input()   